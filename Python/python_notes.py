Function:

key words!

iter: creates an object that is of iterative type
	stages = iter(['alpha', 'beta', 'gamma'])
	**you can use next!! to iter through next steps
	 next(stages)

set: creates a iterable list which is unique
        >>>set('Hello')
        >>> ['H', 'e', 'l', 'o']
        sets are un ordered
        >>> set(123456)
        >>>[1,3,4,2,5,6]

**what is a byte in text editing??


Generators:


set:
    a set is a list where each value is unique! and there is not order
    what are speical uses for sets??
        !!

Random Module:

random.shuffle():
    This method shuffles a sequence, does not return something, it acutally changes the value of the sequence!
    >>>y = list('hello')
    >>>y
        >>> ['h','e','l','l','o']
    >>>random.shuffle(y)
        >>> ['e','l','o','h','l']
    


os module:
    How to create a direcotry in python:

        if not os.path.exsist.(path):
            os.makedirs(path)

        
